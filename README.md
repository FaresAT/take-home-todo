# Take-Home Assessment - ToDo CLI application

A simple, interactive CLI application made in GoLang.

To demo, run `go run main.go`

The available commands are `add`, `remove`, `view tasks`, `view priorities`